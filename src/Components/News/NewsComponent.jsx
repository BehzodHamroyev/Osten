import './NewsComponents.css'
import React from 'react'

const NewsComponent = (props) => {
    return (
        <div className={`${props.className} d-flex justify-content-center align-items-center`}>
            
            
        </div>
    )
}

export default NewsComponent
